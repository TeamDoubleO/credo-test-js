import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: '10%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  buttonContainer: { flexDirection: 'row', gap: '10%', marginTop: '5%', marginLeft: '40%' },
  title: { marginBottom: '10%', fontSize: 24, fontWeight: 600, color: '#464646' },
});
